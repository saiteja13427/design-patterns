#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <memory>
using namespace std;

/*

Groovy styles builders would be one where you can build objects in groovy styles.

Say, you can build a p tag with img tag inside it like

p {
    img {
        "https://saiteja.com/sai.png"
    }
}

To achieve this one should declare say a tag class and then p, img class inhriting from tag class and having possible constructors

*/

int main(){
    
}